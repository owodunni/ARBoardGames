using UnityEngine;

public class BallController : MonoBehaviour {

    public float thrust = 1000f;
    public Rigidbody rb;

    void Start()
    {
		//rb.AddForce(0,0,30);
    }

	    void FixedUpdate()
    {
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
